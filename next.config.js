/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['i.ibb.co.com'], // Add the domain of your external image here
  },
  reactStrictMode: false,
};

module.exports = nextConfig;